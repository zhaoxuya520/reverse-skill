# fixtures

`sample.apk` is a **tiny placeholder** used only so `case-init --preset offline-sample` has a path to put in `scope.md`.

It is **not** a real application and is **not** decompiled by the demo.
